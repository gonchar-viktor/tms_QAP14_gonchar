create an account on https://github.com
open the PyCharm application
now we are creating a project, for this we click "File"->"New project"
enter the name of the project and click "create"
installing packages "pip install -r requirements.txt", "pip install pytest", "pip install allure-pytest"
or you can add a project from an existing repository using the command "git clone <repository link>"